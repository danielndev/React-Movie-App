import React from 'react';

var MoviePage = ({
    image, 
    title,
    description,
    release 
}) => {
    return(
        <div className="MovieCard">
            Working
        </div>
    );
}

export default MoviePage;